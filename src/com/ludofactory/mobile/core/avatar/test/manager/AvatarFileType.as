/**
 * Created by Maxime on 15/12/15.
 */
package com.ludofactory.mobile.core.avatar.test.manager
{
	
	/**
	 * This is used when we want to update the avatar asset files in order to know where e have to store the
	 * file on the device (because files to update are processed in the same routine).
	 */
	public class AvatarFileType
	{
		/**
		 * Armatures file type. */
		public static const ARMATURES:String = "armatures";
		
		/**
		 * Items files type. */
		public static const ITEMS:String = "items";
		
	}
}